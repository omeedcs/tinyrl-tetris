#pragma once

namespace Tetris {

    const double FRAMERATE = 60.0;
    const double TICK_RATE = 1.0 / FRAMERATE;
    const int BOARD_HEIGHT = 20;
    const int BOARD_WIDTH = 10;
    const int PIECE_SIZE = 4;
}
